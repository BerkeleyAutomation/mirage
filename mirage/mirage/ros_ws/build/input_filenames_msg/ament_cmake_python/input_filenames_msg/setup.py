from setuptools import find_packages
from setuptools import setup

setup(
    name='input_filenames_msg',
    version='0.0.0',
    packages=find_packages(
        include=('input_filenames_msg', 'input_filenames_msg.*')),
)
